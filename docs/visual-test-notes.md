Visual test checkpoint (2026-08-18, second capture after ABI repair): /tmp/xeo-chat-thread-current-wait.png still renders a large blue chevron-like mark on white. The server returns HTTP 200 and server-rendered HTML contains Xeo Forge, the opening prompt, `chat setup required`, and `Continue the conversation`; Chromium dump-dom also contains these markers. Therefore HTML is present, but headless screenshot is not a reliable visual pass yet. Do not claim the UI is visually verified. Investigate the blue mark/CSS or use a real browser/window capture before release.
